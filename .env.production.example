# Copy to .env.production and replace with your deployed HTTPS Express API.
VITE_API_BASE_URL=https://your-backend-domain.example.com/api
# Optional base URL for GitHub Pages. Use only when deploying under a repository path.
# VITE_BASE_URL=/mini-project/
